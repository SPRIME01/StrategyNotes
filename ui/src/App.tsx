// Ponytail: shell only. Real atomic UI (atoms/molecules/organisms per
// SPEC sec 11) lands in Phase 12. This component exists to prove the
// React+TS+Vite harness compiles, renders, and is testable.
export function App() {
  return (
    <main>
      <h1>StrategyNotes</h1>
      <p>Local-first, markdown-native strategic knowledge and execution system.</p>
      <p>The note is the atom. Strategy is the grammar. Time is the cost. Evidence is the proof.</p>
    </main>
  )
}
